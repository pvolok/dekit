---
title: dekit kernel status
cli: dekit kernel status
order: 1
related: [cli/kernel, cli/runner/status, config/kernel]
---

Shows the kernel selected for the current project, the one its runner is
running, and the registered default. It also says when the runner needs a
restart to pick up the selection.

:::usage
