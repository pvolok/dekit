---
title: dekit kernel
cli: dekit kernel
order: 16
related: [config/kernel, cli/runner/upgrade]
---

The kernel is the dekit binary that runs a project's runner: the `kernel:`
pin in `dekit.yaml`, else the registered default, else the binary you ran.
See |config/kernel|.

:::usage
