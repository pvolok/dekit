# dekit

A scriptable process manager you drive from a CLI, TUI, API, or script.

```sh
cargo install dekit
```

Prebuilt binaries, an install script, and an npm package are also available — see
the project page for all installation options.

- Repository: https://github.com/pvolok/dekit
- Documentation: https://dekit.run
